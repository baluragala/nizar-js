/*
if
if else
if else if
switch
*/

if (myvar === 0) {
  // then
}

if (myvar === 0) {
  // then
} else {
  // else
}

if (myvar === 0) {
  // then
} else if (myvar === 1) {
  // else-if
} else if (myvar === 2) {
  // else-if
} else {
  // else
}

var fruit = "banana";
switch (fruit) {
  case "banana":
    console.log("banana");

  case "apple":
    console.log("apple");

  default:
    // all other cases
    console.log("not known");
}
